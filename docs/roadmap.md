# Roadmap

Tài liệu này chỉ theo dõi trạng thái ở cấp độ phase — bảng dưới là tóm tắt, chi tiết đầy đủ từng
phase (bối cảnh, quyết định, verify sống) nằm ở [`docs/roadmap/`](roadmap/) (mỗi phase 1 file,
tách ra 2026-08-24 vì file gộp đã quá dài — trước đó là 1 file duy nhất ~2400 dòng). Với một
feature nhỏ hơn một phase, xem `docs/features/`; về ownership/process của team, xem
`docs/team-charter.md`, `docs/CONTRIBUTING.md`, và `docs/agile-process.md`; checklist chi tiết ở
mức UI/UX cho frontend, xem `docs/frontend-checklist.md`.

## Trạng thái hiện tại (cập nhật 2026-08-29 — nền tảng GraphQL/gRPC cho WAAP + BFF gateway thật)

| Phase | Status |
|---|---|
| [0. Skeleton](roadmap/00-skeleton.md) | Đã xong |
| [1. Production-shaped Platform Kernel](roadmap/01-production-kernel.md) | Đã xong |
| [2. Metadata Compiler](roadmap/02-metadata-compiler.md) | Đã xong |
| [3. Permission Engine](roadmap/03-permission-engine.md) | Đã xong |
| [4. Query Planner V1](roadmap/04-query-planner-v1.md) | Đã xong |
| [5. Workflow Engine V1](roadmap/05-workflow-engine-v1.md) | Đã xong |
| [6. Frontend Core](roadmap/06-frontend-core.md) | Đã xong (chưa verify trên browser) |
| [7. Module Migration Strategy](roadmap/07-module-migration-strategy.md) | Đã xong — 4/4 module (crm.customers, sales.orders, inventory.movements, accounting.journal) |
| [8. Hardening](roadmap/08-hardening.md) | Đang làm — **2026-08-28: thêm `AwsSecretsManagerStore`/`GcpSecretManagerStore`** (2 impl `SecretStore` mới cạnh `VaultStore`/`EnvStore`, cùng shape payload `{"dsn": ...}`), `metap_control::build_secret_store(&AppConfig)` gom logic chọn backend (trước đây lặp lại ở từng `main.rs`) — chưa chốt target production cụ thể (cloud provider nào sẽ dùng thật) nên vẫn "đang làm", không phải "xong"; load test + backup/restore drill xong 2026-08-17 |
| [9. Multi-Service Evolution](roadmap/09-multi-service-evolution.md) | Trigger-based, đã rà soát lại 2026-08-17 — vẫn chưa trigger nào xảy ra, không có việc để làm |
| [10. Monorepo, npm publish](roadmap/10-monorepo-npm-publish.md) | Làm một phần |
| [11. Low-code Platform Backbone Architecture](roadmap/11-lowcode-platform-backbone.md) | Phase A + Phase B xong 2026-08-17 (Phase B's "policy editor UI" hoá ra đã có sẵn từ Phase 15); Phase C bắt đầu 2026-08-20 — metadata audit log, migration-impact check, import/export, operational visibility (cross-entity audit feed) xong (2026-08-22); **2026-08-27: `/admin/lowcode/*` + `/platform/tenants*` + toàn bộ route tĩnh của `metap-http` vào OpenAPI/FE codegen** (14 → 54 path ở `/metadata/openapi.json`, PR #4, chuẩn bị cho FE build UI admin low-code), tiện thể sửa gap `GET`/`DELETE` thiếu trên `/api/{entity}/{id}`; babysit `PR #4` tới xanh còn dọn thêm 8 vấn đề CI không liên quan trực tiếp, mỗi cái lộ ra sau khi cái trước được fix vì `cargo test --workspace -- --ignored` không có `--no-fail-fast` (toolchain drift, `aws-sdk-s3` deps, prettier drift, thiếu Redis service, race `control.tenants` trong test dedicated-db, gap lớn nhất — `vault_store.rs`'s e2e test chưa từng chạy được trong CI vì thiếu Vault service, fix bằng service `vault` + step provision AppRole qua `curl`/`jq` — sau đó lộ tiếp race thứ 7 ở `metap-cron`'s `claim_due_retries` test [fix: `static Mutex` theo mẫu `INDEX_BUILD_LOCK` có sẵn], rồi thứ 8: 3 test "manual benchmark" tự ghi rõ "not part of a normal e2e run" trong `crud_service_postgres.rs` bị `-- --ignored` gom nhầm vào chạy — fix bằng `--skip` đích danh); còn lại approval workflow ("nếu cần", chưa có trigger) và schema isolation cấp tenant (chặn bởi quyết định kiến trúc lớn hơn — xem `docs/team-charter.md`'s "Metadata low-code theo từng Tenant") |
| [12. Rust Core Migration](roadmap/12-rust-core-migration.md) | Đã quyết định; Migration Order (bước 1-9) đã xong trong `crates/`; chưa cut over sang production |
| [13. Dynamic Cron Jobs](roadmap/13-dynamic-cron-jobs.md) | Backend đã xong; admin UI đã xong (Phase 15) |
| [14. Multi-language (i18n)](roadmap/14-i18n.md) | UI chrome + locale storage đã xong; metadata-label translation hướng đã chốt (translation-key/override table, 2026-08-22), chưa code — chưa có trigger |
| [15. Shared App Shell (UI kit, real login, permission-aware components)](roadmap/15-shared-app-shell.md) | Đã xong |
| [16. Multi-tenant SaaS Control Plane & Data Plane](roadmap/16-multi-tenant-saas.md) | Hướng B đã chốt. Giai đoạn 1-3 xong (Router, `provision-tenant`+`DedicatedDb`, HTTP tenant provisioning + platform-superadmin — 2026-08-16 → 2026-08-17); Giai đoạn 4: `VaultStore` (token) xong 2026-08-17, AppRole auth + auto-renewal + role lookup/RBAC qua Router (đóng bug login vỡ cho `dedicated_db`) + delete/deprovision tenant xong 2026-08-20 → 2026-08-21; `schema`/trial vẫn chưa có isolation thật; dynamic Vault creds/data-plane/capabilities/FE onboarding/deployment còn lại |
| [17. Metadata-driven Workflow Engine](roadmap/17-metadata-workflow-engine.md) | Increment 1 (on-transition trigger cho `metap-cron`) xong 2026-08-21; Increment 2 (`TargetType::Steps` — chuỗi activity tuần tự, bảng `workflow_runs`) xong 2026-08-28; **Increment 3 (`wait_event` — durable pause chờ 1 domain event khớp pattern, không timeout) xong 2026-08-28** — `TargetType::WaitEvent` mới, chỉ hợp lệ như step trong `steps`; `cron-scheduler::trigger`'s listener khớp thêm chain đang `"waiting"` (song song việc fire job mới, không đụng nhau). **Verify sống đầy đủ qua HTTP+RabbitMQ+Postgres thật cả 3 increment** (boot thật `crm-server`+`outbox-publisher`+`cron-scheduler`, không phải test giả lập): pause đúng lúc chạy tới `wait_event`, resume đúng khi event khớp tới, record đích chuyển trạng thái thật — cộng 4 e2e test mới phủ store layer. `cargo build/clippy -D warnings/fmt --check/test --workspace` sạch toàn workspace |
| [18. Organization & Identity — P0](roadmap/18-organization-identity-p0.md) | Done 2026-08-22 — `RequestContext.context_attributes` (opt-in `AUTH_CONTEXT_ENTITY`, cache + invalidate endpoint), entity mẫu `hr.departments`/`hr.employees` qua low-code, org-scoped policy verify sống qua HTTP thật. P1 (`hr.positions`/`hr.locations`, `managerId` self-reference)/P2 (Legal Entity, Approval Authority...) vẫn proposed, chưa có trigger |
| [19. Table-per-entity](roadmap/19-table-per-entity.md) | 5/5 bước code-complete + e2e (2026-08-23). Wire thật vào `crm-server`: `crm.customers` ở Phase 36 (2026-08-25), **3 entity còn lại (`sales.orders`/`inventory.movements`/`accounting.journal`) xong ở Phase 45 (2026-08-28)** — cả 4 entity code-authored của `crm-server` giờ đều dùng bảng riêng. `apps/jira-server` (Phase 21) vẫn là app demo khác dùng bảng riêng từ đầu. Mọi entity low-code vẫn dùng `records` chung — ngoài phạm vi (xem Phase 45) |
| [20. Backend test kit (regression/performance/security)](roadmap/20-backend-test-kit.md) | Done phần lớn — security (`cargo audit`+CI, tenant-isolation/JWT/RBAC-ABAC test, CodeQL+Semgrep) và performance (k6 qua Docker + Grafana) xong; **2026-08-24 thêm OWASP ZAP (DAST)**, `testing/security/zap/run.sh`, chạy tay không CI. **2026-08-25 (Phase 34) đóng nốt**: Semgrep wired vào CI (blocking), criterion micro-benchmark nightly (`plan_list`/`compile`+`diff`), `seed_10m.sql`. Còn lại: Trụ Regression vẫn chỉ index, chưa có trigger |
| **21. Demo `jira-server` — table-per-entity thật** | Done 2026-08-23 — 2 entity ban đầu (`jira.projects`/`jira.issues`), lần đầu `reconcile()` chạy trong boot sequence thật. Đây là 1 trong 7 phase (21/24/26/29/30/31/32) làm đầy app demo Jira — cả 7 nay là lịch sử build-out của `metap-demo-jira` (repo riêng từ Phase 51), chi tiết đầy đủ ở `../metap-demo-jira/docs/roadmap/`, không giữ ở đây nữa |
| [22. `metap-storage` (object storage)](roadmap/22-metap-storage.md) | Done 2026-08-23 — `ObjectStore`/`S3ObjectStore` (SeaweedFS backend), wire thật ở Phase 27 |
| [23. `metap-cache` (caching layer)](roadmap/23-metap-cache.md) | Done 2026-08-23 — `Cache`/`MokaCache`/`RedisCache` (DragonflyDB), consumer đầu tiên: `PermissionService::with_cache` (policy-row cache) |
| **24. Xây đầy demo jira-server** | 2026-08-23 → 08-24 — sprint/comment/4-state kanban workflow, dashboard+kanban board FE. Chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| [25. Tenant auth pluggable (Bearer + Basic + OIDC)](roadmap/25-tenant-auth-pluggable.md) | Done 2026-08-24 — cả 3/3 bước (crate `metap-auth`, bảng `tenant_auth_configs`, refactor local login; HTTP Basic per-request; OIDC redirect/callback + JIT provisioning + FE `OidcCallbackPage`/SSO button), verify sống đầy đủ kể cả full HTTP round-trip qua fake IdP |
| **26. Làm đầy demo jira-server/jira-fe — auth thật, issue detail+comment, backlog, attachment** | Done 2026-08-24 — attachment ở phase này là consumer thật đầu tiên của `metap-storage` (sau đó generalize ở Phase 27, vẫn ở `metap`). Chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| [27. Generalize attachment thành năng lực platform (`metap-http`)](roadmap/27-attachments-platform.md) | Done 2026-08-24 — crate `metap-attachments` (2 cơ chế: bảng chung + bảng riêng theo entity), route generic `/api/{entity}/{id}/attachments*`, xoá bespoke cũ khỏi jira-server. Đánh đổi: mất bảo vệ chặn-xoá-khi-còn-attachment (đã verify + ghi nhận). Phát hiện + **đã fix** 1 gap migration thật (`0019`'s backfill làm tenant jira kẹt ở migration 18 — sửa bằng cách tái tạo tạm `control.tenants` rồi chạy migrator thật, không sửa nội dung migration đã áp) |
| [28. `dev-tools` tenant-aware](roadmap/28-dev-tools-tenant-aware.md) | Done 2026-08-24 — `seed-admin`/`create-user` giờ đi qua `Router` (không còn ghi nhầm DB cho tenant `dedicated_db`), `mint-token` cảnh báo rõ khi default tenant chưa provision thay vì âm thầm |
| **29. Demo jira — sub-task (self-reference), story points, labels** | Done 2026-08-24 — `parentIssue` là Reference tự tham chiếu đầu tiên trong codebase (verify sống FK/filter/delete-guard đúng — capability này ở `metap-reconciler`, vẫn trong `metap`). Chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| **30. Demo jira — epics, issue type, issue linking, comment edit/delete, search** | Done 2026-08-24 — chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| **31. Demo jira — assignee picker, logwork, watchers, sprint burndown report** | Done 2026-08-25 — route generic `GET /api/{entity}/{id}/workflow-events` (`metap-workflow::list_events` mới) là capability platform, vẫn ở `metap`. Chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| **32. JQL query engine, advanced search, logwork report, dashboard charts** | Done 2026-08-25 — `metap-query::jql` (query language generic, validate theo entity metadata, không phải jira-specific — vẫn ở `metap`) + `?jql=` trên route `/api/{entity}` chung; `BarChart` component (nay ở `@metap/platform-ui`, repo riêng). Phần demo (`AdvancedSearchPage`/`LogworkReportPage`/`DashboardPage`) chi tiết ở `../metap-demo-jira/docs/roadmap/` (xem note Phase 21) |
| [33. Customizable dashboard — layout, widget catalog, org default](roadmap/33-customizable-dashboard.md) | Done 2026-08-25 — crate `metap-dashboards` (bảng `dashboard_configs`, 2 cấp: per-user override + per-tenant default admin-only), route generic `GET/PUT /dashboards/me`+`/dashboards/tenant-default`, `CustomizableDashboardPage` (`jira-fe`, route `/dashboard/custom`) — widget catalog v1 (bar chart, stat tile theo JQL), kéo-thả/resize qua `react-grid-layout` (dependency riêng `jira-fe`, không đẩy vào `platform-react`). Hoàn thành cả 5/5 mục chủ dự án nêu ở Phase 32 |
| [34. Backend test kit — đóng nốt Semgrep CI + criterion nightly](roadmap/34-backend-test-kit-completion.md) | Done 2026-08-25 — Semgrep wired vào CI (blocking, `# nosemgrep` inline suppress finding false-positive duy nhất), criterion micro-benchmark cho `plan_list`/`compile`+`diff` (`.github/workflows/nightly-benchmark.yml`, report-only), `testing/performance/seed_10m.sql` mới (verify bằng bản thu nhỏ). Đóng 2/2 việc "chưa làm" của Phase 20 |
| [35. `metap-lowcode-http` Router-aware](roadmap/35-lowcode-http-router-aware.md) | Done 2026-08-25 — mọi handler resolve `Router::pool_for(tenant)` thay vì đọc thẳng `state.pool` (trước đây `DedicatedDb` tenant sẽ ghi nhầm entity low-code vào DB nền tảng). Không đổi chữ ký hàm `metap-lowcode`. Verify sống đầy đủ qua `crm-server` (tenant `Schema` không đổi hành vi). Chưa wire router vào `jira-server` — bước riêng nếu muốn custom field qua low-code cho jira thật |
| **36. Demo `crm.customers` — table-per-entity thật** | Done 2026-08-25 — entity code-authored đầu tiên của demo crm-server chuyển sang table-per-entity, verify sống đầy đủ qua HTTP thật (capability `metap-reconciler` dùng vẫn ở `metap`). 1 trong 2 phase (36/45) là lịch sử build-out của `metap-demo-crm` (repo riêng từ Phase 51), chi tiết đầy đủ ở `../metap-demo-crm/docs/roadmap/` |
| [37. EventBus — reconnect-with-backoff cho subscribe](roadmap/37-eventbus-resilient-subscribe.md) | Done 2026-08-25 — `metap_infra::run_resilient_consumer` (hàm mới) thay hết shape "bail khi mất kết nối, chờ process manager restart" ở cả 3 consumer thật (`notification-worker`, `cron-scheduler`'s executor + trigger-listener) bằng reconnect tự động backoff exponential capped 30s. Verify sống bằng `docker restart` RabbitMQ giữa chừng — tự phục hồi không cần restart process. Còn lại (chủ dự án xác nhận đáng làm): delivery thật, handler registry chung, retry-backoff per-message |
| [38. `metap-cron` — generic record-event triggers](roadmap/38-generic-record-event-triggers.md) | Done 2026-08-25 — trả lời câu hỏi "handler RabbitMQ dynamic/customize kiểu gì": cơ chế đã có sẵn (`on_transition` trigger, Phase 17), chỉ mở rộng thêm `on_record_event` (created/updated/deleted) thay vì xây mới. Fix tiên quyết: `emit_created/updated/deleted` thiếu `tenantId` trong payload. `cron-scheduler`'s trigger-listener mở routing key sang `#`, tự phân loại. Verify sống full round-trip HTTP→RabbitMQ→cron-scheduler thật + 4 e2e test mới. Bug phụ tìm được: `metap-workflow` thiếu sqlx feature `macros` (chỉ "chạy được" nhờ unification tình cờ) |
| [39. Retry-with-backoff per-message + `TargetType::Email`](roadmap/39-retry-with-backoff-and-email-target.md) | Done 2026-08-26 — `metap_infra::RetryPolicy`/`ConsumedEvent::retry_or_give_up` (TTL+DLX delay-queue pattern, đọc `x-death` cho retry count) dùng đầu tiên ở `cron-scheduler::trigger.rs` (lỗi DB tạm thời khi match trigger giờ retry 3 tier 5s/30s/2m thay vì dead-letter ngay). `TargetType::Email` thứ 4 (gửi qua `lettre`, tái dùng hệ trigger Phase 38, không sửa `notification-worker`) — vá kèm gap `CronJobDuePayload` thiếu trigger context (`recordId`/`entity`). Verify sống: e2e RabbitMQ thật cho retry (`crates/metap-infra/tests/retry_policy_rabbitmq.rs`), full round-trip HTTP→RabbitMQ→cron-scheduler→SMTP thật (Mailhog) cho email, đúng body có record đã trigger |
| [40. `HandlerRegistry` + fix bug mất `routing_key` khi retry](roadmap/40-handler-registry-and-retry-routing-key-fix.md) | Done 2026-08-26 — đóng nốt việc cuối trong list Phase 37 ("handler registry chung"): 1 process tự đăng ký nhiều handler tùy biến trên 1 subscription (`.on(pattern, handler)`, AMQP topic wildcard), khớp nhiều handler chạy song song, ack khi tất cả OK. **Bug thật tìm được khi build test 2** (đã sửa): retry-tier's dead-letter-routing-key ghi đè `routing_key` của message redeliver, làm `classify_topic`/`topic_matches` fail match — **ảnh hưởng ngược Phase 39 đã commit**, retry-with-backoff của `cron-scheduler::trigger.rs` trước fix này mất trigger ngay ở lần retry đầu thay vì thử lại. Fix bằng header `x-original-routing-key` riêng, khoá lại bằng assertion mới trong test Phase 39. Verify sống đầy đủ qua RabbitMQ thật, cả 3 e2e test (2 mới + 1 cũ) pass |
| [41. Fix 12 bug từ AUDIT_2.md](roadmap/41-audit-2-fixes.md) | Done 2026-08-26 — audit toàn bộ `crates/` do 6 agent viết, 1 fork verify độc lập 12 finding nghiêm trọng nhất trước khi sửa (12/12 CONFIRMED). Bảo mật: SQL injection qua `field.name` chưa validate, Basic auth không ràng buộc tenant thật. Data integrity: keyset pagination mất row khi sort NULL, double-JSON-encode fallback ở `WidenType` migration. Tooling: `explain_policies` bỏ qua `PolicyEffect`. Operational: `outbox-publisher` không reconnect RabbitMQ (verify sống bằng `docker restart`), `cron-scheduler` không idempotent khi redeliver, orchestrator/`claim_due_retries` không reclaim khi worker chết giữa chừng. Structural: reconciler backfill/quarantine không tenant-scope (verify sống 2-tenant-1-table). Workflow: `transition()` không cross-check `enumValues`, guard thiếu Gt/Lt (sửa luôn bug thật ở `journal_entry_entity.rs`'s `Neq 0` chấp nhận nhầm số âm). Build/clippy/test sạch xuyên suốt, boot thật `crm-server`+`jira-server` xác nhận không entity nào bị 2 validation mới chặn nhầm |
| [42. Workflow engine — transition payload, `validator`, `set_fields`](roadmap/42-workflow-engine-payload-validator-set-fields.md) | Done 2026-08-26 — mô hình Jira-style condition/validator/post-function cho `WorkflowTransition`: transition giờ nhận `data` payload thật (trước chỉ có `version`), `validator` (`PolicyCondition` chạy trên dữ liệu đã merge payload, khác `guard` vốn chỉ thấy dữ liệu cũ), `set_fields` (post-function khai báo, tái dùng `PolicyValue::Literal`/`FromContext` — cố ý không cho chạy code tuỳ ý để giữ `metap-workflow` entity-agnostic). `compiler.rs` cross-check `set_fields` (field tồn tại, không được ghi đè `state_field`). Verify sống: e2e mới cho transition 2 bước (thiếu payload → `422 validator_failed`; đủ payload → thành công, `set_fields` tự gán đúng từ context), full HTTP lifecycle test cũ pass nguyên (request shape cũ không kèm `data` vẫn hoạt động) |
| [43. Entity field validation — `min`/`max`, `minLength`/`maxLength`](roadmap/43-field-min-max-length-validation.md) | Done 2026-08-27 — `EntityField` thêm 4 field optional (`min`/`max` cho `Number`/`Money`, `minLength`/`maxLength` cho `String`), gap đã ghi nhận từ lâu trong `validation.rs`'s doc comment ("không có format/length concept"). `compiler.rs` cross-check ở boot time (sai kind, hoặc `min > max`/`minLength > maxLength`, đều lỗi ngay khi validate metadata). `validate_payload` check thật khi ghi record, đếm ký tự UTF-8 (không phải byte) cho độ dài chuỗi. `openapi.rs` đồng bộ cả 2 chỗ generator (schema mô tả `EntityField` và schema mô tả record `data`). 11 test mới (6 ở `validation.rs`, 5 ở `compiler.rs`). Còn lại: không có `pattern`/format constraint (ngoài phạm vi yêu cầu), FE `generated-types.ts` chưa regenerate |
| [44. `reconciler-orchestrator` — chạy orchestrator thật lần đầu](roadmap/44-reconciler-orchestrator-service.md) | Done 2026-08-27 — crate mới `metap-reconciler-orchestrator` (binary `reconciler-orchestrator`), ticker thật đầu tiên chạy `metap-reconciler::orchestrator`'s primitives (`claim_due`/`run_claimed_batch`/wave rollout) — trước đó chỉ là thư viện, chưa từng chạy như 1 service (đúng gap CLAUDE.md từng ghi). Claim `(tenant, entity)` từ `reconciler_entity_deployments` → resolve định nghĩa qua `metap_lowcode::get_published` (chỉ entity DB-authored đã publish — nguồn duy nhất 1 ops binary generic đọc được mà không link business code) → `reconcile()` vào bảng riêng qua `Router::pool_for(tenant)`. `metap_reconciler::orchestrator::enqueue_deployment` (hàm mới, bản single-tenant của `advance_wave`) + `dev-tools enqueue-reconcile <tenantId> <entityName> <desiredVersion>` là cách kích hoạt thủ công (chưa có HTTP publish/rollout API — chưa có nhu cầu). `pnpm worker:reconciler:rs`. Verify sống đầy đủ: 3 e2e test mới (`crates/reconciler-orchestrator/tests/`, happy path tạo bảng thật + hội tụ về 0 việc ở lần chạy 2, entity chưa publish → `blocked` đúng, hàng đợi rỗng → no-op) + 1 e2e mới cho `enqueue_deployment` (`crates/metap-reconciler/tests/`), cộng chạy binary thật (không phải test): boot → poll → claim → reconcile fail đúng lý do → không claim lại → `SIGTERM` shutdown sạch. `cargo build/test/fmt/clippy -D warnings` sạch xuyên suốt. Phạm vi cố ý hẹp (ghi rõ trong doc comment crate): chỉ entity low-code đã publish (entity code-authored vẫn theo đường cũ, không đổi), không tenant-fan-out cho `DedicatedDb` (chỉ poll pool chung), không topo-sort FK giữa nhiều entity trong 1 batch, không có API publish/wave-rollout qua HTTP — đóng đúng 1 gap "orchestrator chưa từng chạy", không phải toàn bộ tầm nhìn §6. **2026-08-28 đóng nốt cả 3 gap còn lại**: `metap_reconciler::orchestrator::topo_sort_waves` (Kahn's algorithm theo tenant, entity bị tham chiếu reconcile trước entity tham chiếu nó khi cả hai cùng nằm trong 1 batch — tránh race FK-target-chưa-tồn-tại của `buffer_unordered`; cycle rơi vào 1 wave chung, best-effort; 5 unit test pure-function); `run_tick` (mới) fan-out thêm 1 lượt `run_once` riêng cho từng tenant `DedicatedDb` đang `Active` (mỗi tenant loại này có bản `reconciler_entity_deployments` riêng trong DB của chính nó, trước đây không ai poll — cùng loại gap `outbox-publisher` đã có, đóng theo đúng cách đó); `POST /platform/reconciler/wave-rollout` (`metap-control-http`, `PlatformAdminContext`) bọc `advance_wave` qua HTTP. Kèm fix liên quan: `dev-tools enqueue-reconcile` trước đây luôn ghi vào pool chung `DATABASE_URL` — sai với tenant `DedicatedDb` vì `run_tick` giờ poll riêng DB của tenant đó — sửa bằng `Router::pool_for(tenantId)`. 1 e2e test mới cho fan-out (dùng database thật thứ hai, `#[ignore]`). **2026-08-28 (phiên khác, có Postgres thật): verify sống xong** — cả 4 e2e của `metap-reconciler-orchestrator` (gồm test fan-out) và toàn bộ e2e của `metap-reconciler` pass, dưới cả test mặc định song song lẫn `--test-threads=1`; `cargo fmt --all --check`/`clippy -D warnings` sạch. `cargo build/clippy -D warnings/fmt --check` sạch toàn workspace; `cargo test --workspace` (unit, không cần DB) pass |
| **45. Demo `sales.orders`/`inventory.movements`/`accounting.journal` — table-per-entity cho 3 entity còn lại** | Done 2026-08-28 — nối tiếp Phase 36, cả 4 entity code-authored của demo crm-server giờ dùng bảng riêng. Phát hiện phụ đáng nhớ: `permission-smoke.sh` (script test tay, không phải engine) có 2 lỗi khiến tưởng nhầm là bug ở `metap-permission`/`metap-http` — điều tra kỹ mới lộ lỗi ở chính script, không phải platform. Chi tiết đầy đủ ở `../metap-demo-crm/docs/roadmap/` (xem note Phase 36) |
| [46. `rust-e2e` ra khỏi CI tự động + sửa gốc 2 test flaky](roadmap/46-e2e-manual-and-flaky-test-fixes.md) | Done 2026-08-28 — trigger: PR #6 (Workflow Engine Increment 2) hit CI đỏ 3 lần liên tiếp ở `rust-e2e`, mỗi lần một lỗi flaky khác nhau không liên quan diff, cộng job này chậm hơn hẳn 4 job kia cộng lại. Sửa gốc 2 lỗi (không phải retry cho qua): `metap-peripherals/tests/peripherals_postgres.rs`'s 2 test "planner thực sự chọn index" — `SET LOCAL enable_seqscan = off` + seed 15.000 row cùng entity (ngưỡng thật đo qua `psql`: 10.000 row là điểm trigram index thắng composite index) + mở rộng `INDEX_BUILD_LOCK` bọc luôn đoạn seed+`EXPLAIN` (2 test nhiễu lẫn nhau khi chạy song song, không chỉ lúc `CREATE INDEX CONCURRENTLY`); `metap-infra/tests/handler_registry_rabbitmq.rs`'s 2 test publish trước khi subscription kịp bind (message bị drop âm thầm) — tăng delay chờ 500ms→2000ms (không phải deterministic wait thật, cần đổi API `run_resilient_consumer` dùng chung nhiều binary, ngoài phạm vi). Verify: 10/10 lần chạy `peripherals_postgres` (nhiều mức test-threads) + 3/3 lần `handler_registry_rabbitmq` pass liên tục sau fix. `rust-e2e` chuyển từ `ci.yml` sang `.github/workflows/e2e-manual.yml` (`workflow_dispatch` only) — cùng convention `nightly-benchmark.yml`, ngang hàng security checklist/performance benchmark trong `testing/`: coverage thật, chạy chủ động, không phải gate mọi commit. `ci.yml` còn 4 job tự động không cần service container ngoài. Cập nhật `testing/regression/README.md`/`testing/README.md`/`docs/CONTRIBUTING.md`; các entry roadmap lịch sử (Phase 8/11/20) giữ nguyên làm bản ghi thời điểm quyết định. `cargo build/clippy -D warnings/fmt --check` sạch toàn workspace |
| [47. Tách frontend library ra repo riêng — `@metap/platform-ui` + `@metap/ui`, gỡ Mantine khỏi `apps/crm-fe`/`apps/jira-fe`](roadmap/47-platform-ui-design-system-split.md) | Done 2026-08-29 — `packages/platform-react` (Mantine) move 100% sang repo riêng `../platform-ui` (`@metap/platform-ui`), build lại toàn bộ UI trên `../design-system` (đổi tên `@ui/ui-lib` → `@metap/ui`, Tailwind+Radix+shadcn-style) thay Mantine. `apps/crm-fe`/`apps/jira-fe` trỏ sang `@metap/platform-ui`/`@metap/ui` qua `link:` (không còn trong pnpm workspace của `metap`), tự gỡ luôn Mantine ở phần code riêng của 2 app + dựng pipeline Tailwind thật (gap phát hiện khi build — CSS output giảm 231KB→~30KB). 2 gap `fs.allow` thật tìm được khi verify (Vite dev server 403 file qua symlink ngoài workspace, cả source TS lẫn CSS build sẵn). `packages/platform-react` xoá hẳn sau khi xác nhận 0 tham chiếu còn lại; `CLAUDE.md` cập nhật theo. Verify: `typecheck`/`lint`/`format:check`/`vite build` sạch cho cả `platform-ui`/`crm-fe`/`jira-fe`; không browser-test (theo policy). Còn lại, cố ý chưa làm: tách `apps/crm-server`/`jira-server`/`crm-fe`/`jira-fe` ra repo riêng |
| [48. Redesign UI phân quyền — ma trận RBAC (cơ bản) + condition builder ABAC (nâng cao)](roadmap/48-permission-admin-ui-rbac-matrix-abac-builder.md) | Done 2026-08-29 — plan trước khi code (`EnterPlanMode`). FE: type `PolicyCondition` thật (`admin/policyCondition.ts`, trước là `unknown`), ma trận role×action cho RBAC (`PermissionMatrix.tsx`, hàng "Everyone" cho policy mở cho mọi role), condition builder đệ quy AND/OR cho ABAC (`ConditionBuilder`/`ConditionNodeEditor`/`AttributePicker`/`ValueEditor`). BE: `GET /metadata/actions` (`EntityAction::ALL` làm nguồn chân lý duy nhất, theo quyết định chủ dự án thay vì hardcode FE) + `PolicyStore::sync_basic_policies` (route `PUT /admin/policies/matrix`, 1 transaction thay hết row basic-shaped của entity — gộp toàn bộ thao tác ma trận thành đúng 1 API call thay vì 1 call/click, tự loại bỏ luôn case "policy dùng chung nhiều role"). 3 bổ sung sau demo: grant/revoke user ngay trong ma trận (expand theo role), search permission xuyên mọi entity (`PermissionSearch.tsx`), label `{entity}_{action}` hiển thị thống nhất ở cả ma trận lẫn kết quả search. Bước 8 (cùng ngày): chủ dự án chốt nguyên tắc kiến trúc "`platform-ui` không viết component/styling mới, phải đưa vào `design-system`" — dọn 3 chỗ vi phạm (`TagsField`/`AttributePicker`'s Input+Chip/`MultiFieldSelect` viết tay ở `platform-ui`) chuyển thành `TagsInput`/`SuggestInput`/`MultiSelect` thật trong `design-system` (kèm test+story, cập nhật `component-status.md`), huỷ tiền lệ "tự dựng tạm khi `@metap/ui` chưa có" ghi trong `platform-ui/README.md` trước đó. Verify: `cargo build/test/fmt --check/clippy -D warnings` sạch (`metap-permission`/`metap-http`/`metap-control`/`metap-query`/`metap-metadata`, mọi fake `PolicyStore` cập nhật theo trait mới), `pnpm typecheck/lint/format:check` + `vitest` sạch `platform-ui`+`design-system`, `vite build` thật `crm-fe`+`jira-fe` pass |
| [49. Nền tảng GraphQL + gRPC cho metap — chuẩn bị cho các microservice WAAP](roadmap/49-graphql-grpc-jwks-foundation.md) | Done 2026-08-29 — trigger thật: chủ dự án sắp xây "WAAP" (nhiều microservice thật, không phải demo) — đúng 2 trigger GraphQL BFF/gRPC đã ghi ở Phase 9 nhưng chưa từng xảy ra. Phạm vi chốt trước khi code (`EnterPlanMode`): chỉ thêm năng lực dùng lại được vào `metap`, không làm portal/entity WAAP thật. 5 crate mới: `metap-jwks`/`metap-jwks-http` (JWKS Ed25519 đa-service, rotation 3 bước, thay `CRON_SERVICE_JWT`-kiểu thô sơ), `metap-grpc` (`RecordService` generic CRUD-over-gRPC, `protoc-bin-vendored`, port riêng qua `tokio::spawn`), `metap-graphql`/`metap-graphql-http` (schema `async-graphql::dynamic` sinh từ `MetadataRegistry` lúc runtime, DataLoader/complexity-limit/field-mask đều giải quyết được mà không cần code riêng — field-mask "miễn phí" vì resolver đọc thẳng `RecordDto.data` đã mask sẵn trong `CrudService`). Thêm `CrudService::get_many` (batch, phục vụ DataLoader). Tách JWT decode khỏi `axum::FromRequestParts` (`metap_peripherals::decode_access_token` + `metap_control::resolve_request_context`) làm nền cho cả 2 transport mới dùng chung. Verify bằng test e2e thật (Postgres/JWT/HTTP/gRPC thật) ở mọi bước, không test giả lập — build/clippy/fmt sạch toàn workspace, `crm-server`/`jira-server` không bị ảnh hưởng |
| [50. BFF gateway thật xuyên microservice (`graphql-gateway`) — wire GraphQL/gRPC thật vào jira-server/crm-server](roadmap/50-graphql-gateway-real-bff.md) | Done 2026-08-29 — trigger: sau khi wire GraphQL (Phase 49's thư viện) vào `jira-server` lần đầu, chủ dự án chỉ ra đúng đây chưa phải BFF thật (1 service tự phục vụ GraphQL cho chính nó, không phải 1 gateway đứng trước nhiều service). Chốt xây binary gateway thật trước khi code (`EnterPlanMode`). `RecordBackend` trait mới (`metap-crud`) — điểm neo "local-vs-remote dispatch" `docs/architectures/04-strategy.md` từng ghi nhận chưa thiết kế; `CrudService` impl thẳng (in-process), `metap_grpc::GrpcBackend` impl mới (remote qua gRPC, dùng 1 service JWT tĩnh mỗi upstream — tiền lệ `CRON_SERVICE_JWT`, không propagate identity người gọi gốc). `metap-graphql`'s `build_schema`/`RecordLoader` đổi nhận `Arc<dyn RecordBackend>` thay `Arc<CrudService>` — hành vi single-service không đổi. `CompositeBackend` mới (`metap-graphql`) route theo entity name tới đúng backend. Crate mới `crates/graphql-gateway` (binary `graphql-gateway`): đọc cấu hình `UPSTREAM_<N>_{NAME,GRPC_ADDR,METADATA_URL,SERVICE_JWT}`, gọi `GET /metadata/entities` từng upstream qua HTTP thật để dựng 1 `MetadataRegistry` chung (fail fast nếu 2 upstream trùng tên entity), build schema qua `CompositeBackend`, tự phục vụ `axum` app riêng (không dùng `AppState`/`build_router` vì gateway không có Postgres/`CrudService`) — `/health`, `/graphql`, `/graphql/playground` (non-prod). Auth vào gateway chỉ decode token bằng khoá tĩnh riêng, không phải nguồn identity cho downstream (ghi rõ giới hạn này trong code+README). Thêm gRPC vào `crm-server` (trước đó chỉ `jira-server` có) để gateway có 2 upstream thật. Sửa CSP GraphiQL (`security_headers.rs` đổi `headers.insert` → `.entry().or_insert_with()` để route tự set CSP riêng không bị ghi đè) — bug thật user báo qua browser console khi test GraphiQL của `jira-server`. **Verify quan trọng nhất**: 1 test e2e dựng 2 harness thật độc lập (mỗi cái tenant/keypair/CrudService/gRPC listener/REST listener riêng), trỏ gateway vào cả 2, chạy đúng 1 GraphQL query lấy field từ cả 2 entity — assert cả 2 phần dữ liệu có trong 1 response (bằng chứng "BFF thật" chứ không suy luận). `cargo build/clippy -D warnings/fmt --check/test --workspace` sạch toàn workspace, `pnpm dev:jira:rs` verify sống qua curl. Cố ý chưa làm: identity người gọi gateway propagate xuống downstream (cần JWKS trust root chung, `metap-jwks` đã có nhưng `GrpcRecordService::auth` từng service tự chọn verifier riêng), batch RPC thật cho `get_many` (hiện N lời gọi `get` tuần tự, không phải 1 RPC gộp), rate-limit ở tầng gateway |
| [51. Tách `apps/crm-server`/`crm-fe`/`jira-server`/`jira-fe` ra 2 repo riêng](roadmap/51-example-apps-repo-split.md) | Done 2026-08-31 — đúng việc Phase 47 để lại "cố ý chưa làm". 2 repo mới `../metap-demo-crm`/`../metap-demo-jira` (cấu trúc phẳng, không có tầng `data-plane/` như `metap-demo-waf` — không cần multi-plane), copy nguyên trạng qua `git ls-files` (không copy nhầm `.env`/`keys/` local), sửa lại mọi path tương đối lệch cấp (`Cargo.toml`'s `path` dependency, `web/package.json`'s `link:`, `vite.config.ts`'s `fs.allow`, scripts/`.env.example`'s `REPO_ROOT`). Verify build thật (`cargo build`, `pnpm install`+`tsc -b`) cho cả 2 repo **trước khi** xoá bản gốc trong `metap` — an toàn hơn xoá trước verify sau. Xoá hẳn 4 thư mục khỏi `metap` (`git rm --cached`, chỉ stage không commit), root `Cargo.toml`/`package.json` rút gọn theo (root `package.json` chỉ còn 3 script cargo-wrapping — nhóm `db:migrate`/`mint-token`/4 worker/`dev:rs`/`start` xoá hẳn theo yêu cầu chủ dự án, vì tất cả đều `cd apps/crm-server` để có `.env`/`keys/`), `ci.yml`'s job `frontend` xoá (không còn gì chạy), `CLAUDE.md` sửa rộng (~15 chỗ nhắc `apps/crm-server`/`apps/jira-server` trong mọi bullet crate + toàn bộ section "Sample apps"/"Commands"/"Frontend"). Cố ý không quét `docs/roadmap/*.md` lịch sử/doc-comment rải rác trong crate — giữ nguyên làm bản ghi thời điểm, đúng tiền lệ Phase 46 |
| [52. Tách tầng SaaS low-code control-plane ra repo riêng `metap-lowcode`](roadmap/52-split-lowcode-saas-crates.md) | Done 2026-08-31 — phân tích trước (`docs/features/07-split-lowcode-saas-crates.md`), lên plan qua `EnterPlanMode` rồi mới code. 4 crate di chuyển nguyên trạng (`metap-lowcode`/`metap-lowcode-http`/`metap-control-http`/`reconciler-orchestrator`, ~3945 dòng) — verify bằng dependency graph thật: không crate core nào phụ thuộc ngược, `../metap-demo-jira` chạy multi-tenant thật mà không đụng tầng này. **1 điều chỉnh phát hiện lúc lên plan**: KHÔNG tách `metap-control::provisioning.rs` như phân tích ban đầu — `jira-server` (không low-code) vẫn dùng nó để provision tenant, tách sẽ tạo dependency ngược xấu (`dev-tools` phải phụ thuộc lùi vào repo mới). Verify build thật (`cargo build --workspace`) ở cả `metap-lowcode` (path dep 3 cấp `../../../metap/crates/X`, khác `metap-demo-crm`'s 1 cấp vì có tầng `crates/` con) và `metap` sau khi xoá, cộng `metap-demo-crm` (sửa `Cargo.toml` + `main.rs`, consumer thật duy nhất) và `metap-demo-jira` (không sửa gì, verify không vỡ) — cả 4 chỗ build sạch. `cargo test --workspace` ở `metap` pass. `CLAUDE.md` cập nhật theo (bullet `reconciler-orchestrator` rút gọn, bullet `metap-control` sửa 2 chỗ, thêm note mới cạnh note "No example apps" của Phase 51) |
| [53. Đổi tên `metap-lowcode-platform`→`metap-lowcode`, thêm crate `metap-runtime`, mono-repo microservices cho `metap-lowcode`](roadmap/53-metap-runtime-and-lowcode-microservices-plan.md) | In-progress 2026-08-31 — đổi tên repo xong (đúng tên chủ dự án chọn), verify build sạch cả `metap-lowcode`+`metap-demo-crm`. `metap-runtime` (`docs/features/08-metap-runtime-common-crate.md`) **hoàn tất cả rà soát lẫn di trú** (2 lượt trong ngày): rà soát dependency graph thật tìm 3 ứng viên (HTTP client thiếu timeout, bearer-token parse lặp 3 lần 3 error type, env-var-with-default lặp), tạo crate — sau đó di trú thật sang 7 crate (`metap-infra`/`metap-http`/`metap-grpc`/`metap-jwks`/`graphql-gateway`/`cron-scheduler`/`dev-tools`), phát hiện thêm 1 idiom thứ 4 (`env::optional`, 23 lần) và 9 chỗ lặp ở `dev-tools` bị bỏ sót lúc rà soát ban đầu. 2 thay đổi hành vi runtime thật, cả 2 là cải thiện: timeout 30s mặc định cho `metap-jwks`/`graphql-gateway`'s HTTP client (trước đó không có, rủi ro hang thật), `bearer::parse_bearer` khoan dung whitespace thừa. `cargo build/test/clippy --workspace -D warnings` sạch toàn `metap`; `metap-demo-crm`/`metap-demo-jira`/`metap-lowcode` build lại sạch không đổi gì. Phần "mono-repo microservices cho `metap-lowcode`" mâu thuẫn với ADR đã ghi (`09-adr.md`, "không tách microservice cho SaaS multi-tenant") + có blocker kỹ thuật thật (`metap-lowcode-http::apply_registry`'s `.store()` giả định chung process với tiến trình phục vụ entity) — trình 3 phương án, **chủ dự án chốt**: ADR gốc chỉ áp `metap`-core (business CRUD hot path), `metap-lowcode` là repo private SaaS riêng, chọn microservice từ đầu để giao việc song song cho nhiều agent — thêm bullet làm rõ phạm vi vào `09-adr.md`. Thiết kế đầy đủ + task breakdown T1-T6 ở `../metap-lowcode/docs/architecture.md` (registry-distribution blocker giải bằng outbox event `lowcode.registry.changed`, không phải poll mới). **T1 xong** (tách `services/`/`crates/`, move `reconciler-orchestrator`, build sạch); **T2-T6 chưa code** (domain-lib đổi, 2 binary service mới, subscriber trong `metap-demo-crm`, `metap_runtime::bootstrap` sau khi có caller thứ 2). **Bổ sung cùng ngày** (câu hỏi "ngoài `http_error` thì còn gì common có thể vứt vào contrib k"): vòng rà soát 2 tìm thêm 3 ứng viên thật — `http_error` (2 hàm response-shape chuyển từ `metap-http::error` sang `metap-runtime`, `metap-http::error` giờ re-export; sửa luôn 1 bug thật: `graphql-gateway` tự viết response 401 dạng plain-text thay vì đúng shape chung), `shutdown::signal` (y hệt 17 dòng ở 3 binary, `graphql-gateway` thiếu SIGTERM — gap thật), `cors::build` (logic dựng `CorsLayer` gần như y hệt `metap-http`/`graphql-gateway`) — cộng 2 chỗ `env_or` ở `outbox-publisher` bị 2 vòng trước bỏ sót. `metap-runtime` giờ 6 module/19 test, 9 crate phụ thuộc; không thể gộp thẳng vào facade `metap` (sẽ tạo vòng lặp dependency thật). `cargo build/test/clippy/fmt --all --check` sạch toàn `metap`; `metap-lowcode`/`metap-demo-crm`/`metap-demo-jira` build lại sạch **Vòng 3 cùng ngày** (chủ động, không chờ nhắc): thêm `openapi::insert` (3 dòng y hệt lặp ở `metap-http`/`metap-lowcode-http`/`metap-control-http`), migrate cả 2 crate ở `../metap-lowcode` lần đầu depend `metap-runtime`. **Đổi tên `metap-contrib`→`metap-runtime` cùng ngày** (chủ dự án chọn — vai trò thật là SDK viết backend tuỳ biến, không chỉ dedupe) — ~50 chỗ đổi (thư mục, package, `metap_runtime::`, alias facade `metap::runtime`, file feature-brief). Verify từ sạch (`cargo clean` cả `metap`+`metap-lowcode` trước khi build/test/clippy/fmt) — sạch toàn bộ 4 repo. |
| [54. Tách `docs/` ra repo riêng `metap-docs`](roadmap/54-docs-repo-split.md) | Done 2026-08-31 — copy nguyên trạng 76 file tracked (`git ls-files docs`) từ `metap` sang `../metap-docs`, giữ `docs/` làm subfolder (không flatten root) nên mọi link nội bộ giữa các file docs không đổi. Khác 3 lần tách trước: không có compiler bắt lỗi tham chiếu hỏng — khảo sát trước: 150 chỗ trong doc-comment `crates/*.rs` (84 file) + 24 `CLAUDE.md` + 10 `README.md` trích dẫn `docs/...`, chốt (2 câu hỏi rõ trước khi làm) **không sửa lại** — đúng tinh thần "không viết lại lịch sử", chỉ thêm 1 ghi chú rõ ở đầu `CLAUDE.md`/`README.md` của `metap` nói `docs/...` trong comment/text cũ giờ nghĩa là `../metap-docs/docs/...`. Phạm vi chốt trước: chỉ `docs/` của `metap`, không gộp `docs/` riêng của `metap-lowcode`/`metap-demo-crm`/`metap-demo-jira`. Verify: không có CI job nào đọc `docs/` chức năng (chỉ comment trích dẫn) nên xoá khỏi `metap` không vỡ CI |
| [55. `[workspace.dependencies]` cho dependency dùng chung](roadmap/55-workspace-dependencies-centralization.md) | Done 2026-08-31 — grep thật toàn bộ `crates/*/Cargo.toml` (không đoán) tìm 21 dependency được >= 2 crate dùng (`tokio`/`sqlx`/`serde`/`serde_json`/`uuid`/`chrono`/`anyhow`/`tracing`/... ), đưa vào `[workspace.dependencies]` ở root `Cargo.toml`; dependency chỉ 1 crate dùng (`tracing-subscriber`/`lapin`/`argon2`/...) giữ nguyên local. 2 dạng khai báo tuỳ feature set đồng nhất hay không (`tokio`/`sqlx`/`axum`/`tower-http`/`async-graphql` giữ `features = [...]` riêng per-crate vì Cargo chỉ cộng thêm feature qua workspace inherit, không bớt được — nếu để superset chung sẽ ép crate nhẹ như `graphql-gateway` compile thêm feature `sqlx` nó không cần). Migrate 28/29 crate qua script Python (tránh sed/regex thô đụng nhầm comment). `Cargo.lock` xác nhận mỗi dependency vốn đã chỉ 1 version trước khi sửa — centralize ở đây là chuẩn hoá chỗ khai báo, không phải cách giảm `target/`. `cargo build/test/clippy -D warnings/fmt --all --check` sạch toàn `metap`; `metap-lowcode`/`metap-demo-crm`/`metap-demo-jira` build lại sạch không cần sửa gì |
| [56. `metap-runtime` trở thành SDK viết backend tuỳ biến + crate mới `metap-app`](roadmap/56-metap-runtime-sdk-and-metap-app.md) | Done 2026-08-31 — vòng rà soát thứ 4 cho `metap-runtime` (sau 3 vòng Phase 53): thêm `rate_limit`/`trace` (kéo khỏi `metap-http::build_router`, đóng gap `graphql-gateway` thiếu hẳn rate-limit/tracing), `request_id`/`request_context` (move nguyên vẹn), `serve::run` (lặp ở 4 `main.rs`). Chủ dự án tự phát hiện rủi ro vòng lặp dependency trước khi code (bootstrap AppState cần `metap-control`/`metap-permission`/`metap-cache`/`metap-infra`, 4 crate đó đã depend `metap-runtime`) — qua `EnterPlanMode`, chốt crate mới **`metap-app`** cùng tầng facade `metap` (`bootstrap_platform(&AppConfig) -> PlatformParts`, verify không vòng lặp bằng `cargo tree`), thay đúng đoạn boilerplate lặp 3 lần thật (`metap-demo-crm`/`metap-demo-jira`/`templates/metap-app`). Tài liệu hoá pattern viết backend tuỳ biến (router/DB/permission/event) bằng doc-comment + trỏ `metap-lowcode-http` làm ví dụ thật, không viết abstraction mới. Verify `templates/metap-app` qua bản copy scratchpad (Cargo.toml gốc dùng git-URL placeholder cho `cargo generate`), phát hiện + fix 1 bug có sẵn không liên quan (`EntityField` thiếu field `storage` trong 1 test). `cargo build/test (89 ok)/clippy -D warnings/fmt --all --check` sạch toàn `metap` (từ sạch, `cargo clean` trước) và `metap-lowcode`; `metap-demo-crm`/`metap-demo-jira`/`graphql-gateway` build/clippy lại sạch |
| [57. Chuẩn bị `metap-runtime`/`metap-grpc` cho tích hợp Istio/Linkerd — W3C Trace Context](roadmap/57-istio-linkerd-trace-context.md) | Done 2026-08-31 — trigger: chủ dự án hỏi "http, grpc hỗ trợ các header của istio/linkerd chưa", xác nhận mục tiêu "muốn tích hợp được Istio trong tương lai, vì metap-lowcode sau này sẽ rất lớn" (chuẩn bị codebase, chưa deploy mesh thật). Rà soát trước khi lên plan: không có gì tương thích chuẩn mesh — `x-request-id` tự sinh mới thay vì pass-through (phá log correlation Envoy/Istio ingress cần), không đọc/forward B3 hay W3C `traceparent`, `metap-grpc::client::GrpcBackend::signed_request` (điểm nghẽn duy nhất mọi RPC outbound) không gắn gì cho tracing. Qua `EnterPlanMode` trước khi code, chọn W3C Trace Context (không phải B3) — chuẩn IETF cả Istio/Linkerd hỗ trợ, không cần thêm SDK OpenTelemetry (sidecar mesh tự tạo/export span). Module mới `metap_runtime::trace_context` (`TraceContext`, `from_headers`/`to_traceparent_header`, ambient qua `tokio::task_local!`) — cộng thêm, không thay `x-trace-id`/`RequestIds` cũ. Wire vào `request_id::generate_request_ids` (parse `traceparent` đến + sửa `x-request-id` pass-through cho Envoy), `trace::build` (thêm field span `trace_id`/`span_id`), `http_client::attach_trace_context` (helper opt-in, chưa wire caller nào — 3 caller hiện tại đều boot-time/background), và **tự động** vào `metap-grpc::client::signed_request` qua hàm `attach_traceparent` — sửa đúng 1 chỗ là mọi resolver GraphQL/route gọi ra upstream tự động có propagation. Cố ý ngoài phạm vi: fallback B3, gRPC phía server tự `scope()`, `tracestate` header, tích hợp OpenTelemetry SDK/export span thật, tự động hoá `http_client` qua `reqwest-middleware`. `metap-runtime` giờ 11 module/28 test (từ 10/19 ở Phase 56); `metap-grpc` +2 test. `cargo build/test (89 ok, 224 pass)/clippy -D warnings/fmt --all --check` sạch toàn `metap`; `metap-lowcode` build lại sạch |
| [58. `submit_entity!`/`register_all_submitted()` — entity tự đăng ký qua `inventory`](roadmap/58-entity-auto-registration.md) | Done 2026-09-01 — entry ghi lại lịch sử, viết sau khi rà soát phát hiện gap tài liệu (code đã commit ở cả 4 repo trước khi entry này tồn tại). `metap-metadata` thêm `EntityFactory`/`inventory::collect!`, macro `submit_entity!` (gọi cạnh hàm định nghĩa mỗi entity, nộp qua `inventory` lúc link-time) và `MetadataRegistry::register_all_submitted()` — thay hẳn kiểu liệt kê entity 2 lần (`mod` + `registry.register(x_entity())?` tay trong `main.rs`, dễ quên bước 2 mà không lỗi biên dịch). Wired vào `templates/metap-app`. Áp dụng cùng ngày ở cả 3 downstream: `metap-demo-crm` (4 entity), `metap-demo-jira` (8 entity, cố ý giữ nguyên thứ tự `reconcile()` FK-dependency riêng — không liên quan tới thứ tự đăng ký), `metap-demo-waf`/`data-plane` (9 entity + regression test đếm đủ 9, bundle kèm 1 fix thật không liên quan: `data-plane` chưa từng init `tracing_subscriber` nên mọi `tracing::info!` bị âm thầm drop). `metap-lowcode` cố ý không áp dụng — không sở hữu entity code-authored nào, chỉ tiêu thụ 1 `base_registry` đã dựng sẵn từ binary host qua `merge_with`, không phải gap |
| [59. Rà soát atom lạc chỗ ở `platform-ui` + thêm `FileUpload`](roadmap/59-design-system-atom-audit.md) | Done 2026-09-01 — chưa có người review nào ở `design-system` tính tới thời điểm này, quyết định cứ tiếp tục build thay vì chờ. Rà soát chủ động tìm tiếp cùng loại gap Phase 48 (`TagsField`/`MultiFieldSelect` viết tay lẽ ra thuộc `@metap/ui`): **`BarChart`** — UI atom hoàn toàn generic (chỉ nhận `{label, value, color?}`, tự vẽ SVG không phụ thuộc lib chart) bị để nhầm ở `platform-ui/src/charts/`, chuyển hẳn thành `design-system`'s component (8 test), 2 consumer thật (`apps/jira-fe`'s `DashboardPage.tsx`/`CustomizableDashboardPage.tsx`) đổi import. **`FileUpload`** — component mới (click + drag-drop, 12 test), thay `<input type="file">` trần ở `apps/jira-fe`'s `IssueDetailPage.tsx` (1 consumer, xây trước khi có consumer thứ 2, tiền lệ `metap-storage`). Phạm vi validate cố tình giới hạn HTML-level (`accept`/`maxSize`) — cùng ranh giới `Form`/`FormField` đã lập ("lib lo UI + hiển thị lỗi, app lo business rule"), không đọc nội dung file (không verify cert, không parse schema Excel, không quét virus) dù chủ dự án có hỏi thêm về việc đó khi chốt yêu cầu. Phát hiện thật lúc viết test: `accept` re-check ở JS chỉ có tác dụng qua drag-drop, native picker qua click đã tự lọc theo `accept` từ trước. `design-system`: `build:types`/`lint`/`pnpm build` sạch, 44 file/349 test pass. `platform-ui`: `typecheck`/`lint`/`format:check` sạch. `apps/jira-fe`'s `web/`: `tsc -b` sạch, không thêm lint warning mới — đã `pnpm build` lại `design-system` trước verify vì consumer resolve qua `dist/` (symlink), không phải `src/` |
| [60. `GeneratedList`/`RecordDetail` — sửa layout, thêm back-link, tận dụng diện tích màn hình](roadmap/60-generated-list-detail-layout-fixes.md) | Done 2026-09-01 — trigger: phản hồi trực tiếp sau khi dùng UI thật ("list xấu, detail không có back, diện tích không tối ưu"). **Bug alignment cột thật** tìm được khi đọc code: mỗi virtualized row (`@tanstack/react-virtual`) là `position: absolute`, bị loại khỏi normal flow — `table-layout: auto` mặc định chỉ tính độ rộng cột từ row còn trong flow (chỉ còn header), khiến data cell không khớp cột header. Sửa bằng `table-fixed` (chỉ đọc độ rộng từ đúng 1 row đầu — row label — mọi row khác ăn theo, không phụ thuộc absolute hay không) + cột actions width cố định. Polish thêm cùng nguyên nhân "xấu": sort indicator đổi từ ký tự thô `▼`/`▲` sang SVG nhất quán, filter row's `Input`/`Select` thu nhỏ `h-8`. `RecordDetail` thêm back-link (`navAdapter.toRecordList`, hàm có sẵn — `handleDelete` đã dùng nhưng chưa lộ ra UI), key i18n mới `common.backToList` (en/vi). Diện tích: `GeneratedList` thử `w-full` full-bleed trước — ảnh chụp thật từ chủ dự án lộ ra `table-fixed` chia đều cột khiến dữ liệu ngắn bị kéo giãn quá xa trên màn rộng, chốt lại `max-w-6xl` → `max-w-7xl` (tăng vừa phải, không full-bleed); chiều cao bảng từ `h-[600px]` cố định → `h-[calc(100vh-280px)]` + `min-h-[420px]` sàn (280px là ước lượng heuristic, ghi rõ trong code, có thể cần chỉnh lại sau khi nhìn thật); `RecordDetail`'s `max-w-3xl` (768px, chật cho lưới 2 cột) → `max-w-5xl`, cố tình giữ nguyên 2 cột (không thêm cột 3 vì `getFieldLayoutHint`'s `span` chỉ tính cho lưới 2 cột). Cố ý không đụng `AppShellLayout` (shell dùng chung mọi trang, sửa full-height flex rủi ro cao hơn, chưa audit hết mọi trang khác ở 2 app) — chọn cách ít rủi ro hơn cục bộ trong `GeneratedList`. Xác minh: `platform-ui` `typecheck`/`lint`/`format:check` sạch, `apps/crm-fe`/`apps/jira-fe`'s `web/` `tsc -b` sạch. Không tự verify bằng browser (đúng "Frontend verification policy") — giao lại cho chủ dự án kiểm trên browser thật, đặc biệt số `calc(100vh-280px)` có thể cần chỉnh |
| [61. `metap-demo-waf/data-plane` — tách 3 microservice theo pillar + GraphQL gateway](roadmap/61-waf-microservices-split.md) | In-progress 2026-09-01 — trigger: chủ dự án chốt hướng "metap-waf thiết kế microservice, GraphQL gọi xuyên nhiều service". `EnterPlanMode` trước khi code, 2 vòng làm rõ scope (chủ dự án tự chỉ ra trục Admin/Customer Portal — `data-plane/docs/09-access-control.md` — bị bỏ sót, chốt lại phase này chỉ thiết kế Customer Portal backend). **T1 spike phát hiện blocker thật**: `validate_references()` bắt entity đích registered cùng registry, nhưng route `/api/:entity*` generic nghĩa là đăng ký `zone_entity()` ở service khác chỉ để pass validate sẽ đồng thời lộ CRUD `waf.zones` ở đó — chủ dự án chọn `zoneId` là `String` (không `Reference`) ở `scanning-service`/`alerting-service`, cùng workaround `triggeredById`/`assignedTo` đã có. `data-plane/` đổi thành Cargo workspace 3 member (`zones-service` :3000, `scanning-service` :3010, `alerting-service` :3020, path-dep 4 cấp), modernize sang `bootstrap_platform`/`metap::grpc::optional_serve` (Phase 56/58). GraphQL gateway tái dùng nguyên trạng `metap/crates/graphql-gateway`, chỉ dùng cho query cross-service read-only (không mutation — gateway không propagate identity thật). Admin Portal backend (`Plan`/`Subscription` billing) ghi nhận là service khác, chưa thiết kế. Thêm exception ADR riêng cho WAF vào `09-adr.md` (khác `metap-lowcode`: đây vẫn là CRUD hot path thật, ACID vẫn giữ nhờ chung DB — lý do tách là chu kỳ scale khác nhau giữa pillar). Xác minh: `cargo build/test/clippy --workspace` sạch, 3 unit test mới `owns_exactly_its_own_N_entities` pass, **cộng verify sống qua HTTP thật cùng ngày** — tạo `ScanJob` cross-service trỏ `zoneId` (String) thành công, `GET`/`POST /api/waf.zones` 404 thật ở cả `scanning-service`/`alerting-service` (bằng chứng route không lộ, không chỉ suy luận), `relatedDisplay` nội bộ `zones-service` không đổi. GraphQL gateway instance thật cho WAF cũng dựng xong cùng phiên (T4, config-only tái dùng `metap/crates/graphql-gateway`, không code mới) — 1 query gộp `wafZones`+`wafScanJobsList`+`wafIncidentsList` từ cả 3 service trong 1 response, bằng chứng BFF thật. **T5 routing FE cũng xong cùng phiên** — `web/vite.config.ts` viết lại thành middleware tự forward theo tiền tố entity + gộp `/metadata/entities`; bug thật tìm được khi test (không phải đọc code): Vite 8's `server.proxy`'s `router` option không hoạt động như tài liệu mô tả (luôn dùng `target` tĩnh), sửa bằng middleware tự viết forward qua `fetch`, verify lại bằng `curl` thật qua dev server cho cả 3 service + entity list gộp đủ 9. `tsc -b`/`oxlint`/`prettier --check` sạch. T6 (Zone overview, gọi gateway thật từ browser) cũng xong cùng phiên — thêm lớp `useGraphQLQuery`/`graphqlFetch` dùng chung vào `platform-ui` trước (platform-ui trước đó chưa có fetch GraphQL nào, kể cả tổng quát), rồi mới build `ZoneOverviewPage`. 2 bug thật tìm được lúc verify: (1) Vite 8's proxy `router` option không hoạt động (đã biết từ T5); (2) gateway ban đầu dùng keypair riêng — token đăng nhập thật không decode được ở gateway, sửa bằng dùng chung key với 3 service. Verify bằng đúng token đăng nhập thật (không phải token test riêng) qua `curl` tới `/graphql`. **Chưa xong**: Traefik/reverse-proxy thật cho production (dev-server routing chỉ dùng lúc `pnpm dev`), Admin Portal backend riêng. **Cập nhật 2026-09-02 — câu "không mutation, gateway không propagate identity thật" ở trên giờ đã lỗi thời, không còn đúng**: (1) Forward identity người gọi thật — `RequestContext.forwarded_bearer_token` (mới, `metap-permission`), `graphql-gateway/src/server.rs::authenticate` giữ token gốc của caller, `metap-grpc::GrpcBackend::pick_token` ưu tiên forward token đó xuống upstream thay vì service JWT cố định — chỉ chạy được vì gateway + cả 3 service WAF share 1 keypair dev. Verify sống: mutation `updateWafIncidents` qua `/graphql` bằng token user thật → `records.updated_by` đúng user đó; lặp lại bằng user không quyền → `403 forbidden` đúng theo permission thật của upstream (không phải suy luận). (2) Tự động refresh service-account token — phát hiện sống: `UPSTREAM_<N>_SERVICE_JWT` tĩnh mint tay qua `dev-tools mint-token` (TTL cứng 3600s) hết hạn giữa lúc chạy, gateway 401 lúc boot-time schema discovery → crash → `web` domino theo (không resolve được host gateway). Sửa bằng `metap_grpc::ServiceTokenSource` (mới) — gateway tự `POST /auth/login` bằng service-account thật (`dev-tools create-user` + `dev-tools seed-admin`, không hết hạn như JWT) lúc boot, tự refresh nền trước khi token hết hạn, không còn bước dán tay `.env` nào nữa. `UPSTREAM_<N>_SERVICE_JWT` env var đổi thành `LOGIN_URL`/`SERVICE_EMAIL`/`SERVICE_PASSWORD` (`metap/crates/graphql-gateway`, dùng chung toàn org — không riêng WAF). Xác minh: `cargo build/test --workspace` sạch từ `metap/`, 2 e2e test (`grpc_backend_client_postgres`, `gateway_e2e_postgres`) chạy qua `--ignored` pass, cả 2 curl positive/negative check trên vẫn pass sau khi rebuild gateway sang login-based auth (không regression) |
| [62. `RelatedView` metadata + `RelatedRecordsPanel` — thay `ZoneOverviewPage` viết tay bằng khai báo](roadmap/62-related-view-metadata.md) | Done 2026-09-01 — trigger: chủ dự án hỏi thẳng "query không tự parse từ entity à" sau khi thấy `ZoneOverviewPage` viết tay ở Phase 61. Research trước khi lên plan xác nhận không có gì tái dùng được (`metap-dashboards` chỉ lưu JSON blob opaque, widget catalog `jira-fe` hard-code cứng `jira.issues`, `metap-metadata` chưa có quan hệ ngược, `metap-graphql` sinh schema 1 chiều) — cần khái niệm metadata mới thật. **Phát hiện tự sửa trước khi code**: định thêm `related_views` thẳng vào `EntityDefinition` — grep ra ~50 file trên toàn org construct nó bằng struct literal (không `..Default::default()`), sẽ phá compile hết cho 1 capability chỉ 1 entity dùng. Đổi sang đăng ký riêng qua `submit_related_views!` (macro mới, cùng cơ chế `inventory` với `submit_entity!`, map riêng trong `MetadataRegistry`, không đụng `EntityDefinition`) — 0 file cần sửa. Cố tình không validate cross-entity (đúng tiền lệ `FirewallRule.matchCondition`, cùng lý do route-leak Phase 61) — hệ quả: `fields` phải liệt kê tay, không suy ra được "toàn bộ field". `RelatedRecordsPanel` (`platform-ui`, mới) tự build 1 query GraphQL gộp từ `relatedViews` metadata (naming tự tính qua `graphqlNaming.ts`, mirror `metap-graphql`'s thuật toán), wire vào `RecordDetail` tự động — 0 code thêm cho entity mới khai báo. `waf.zones` khai 4 related view (đúng nội dung `ZoneOverviewPage` cũ), xoá hẳn trang viết tay sau khi xác nhận `RecordDetail` tự hiển thị đúng. Xác minh: `metap-metadata` 43 test pass (4 mới), `cargo build/clippy/fmt` sạch toàn `metap` (xác nhận đúng 0/50 file cần sửa), `platform-ui` `typecheck/lint/format:check` sạch + `pnpm generate:types` chạy thật, `metap-demo-waf` build/clippy/fmt sạch cả 3 service. Verify sống qua HTTP thật: `relatedViews` đúng trong `/metadata/entities/waf.zones`, query `RelatedRecordsPanel` tự build trả đúng dữ liệu thật từ cả 3 service qua gateway lẫn qua dev-server proxy |

## Định hướng chưa lên phase (chưa có trigger)

Tám ý nảy sinh từ thảo luận kiến trúc, hợp lý về sản phẩm nhưng chưa có trigger cụ thể nên chưa
được lên thành phase: workflow hai chế độ (in-process + cross-module), workflow
visualize/hướng BPM nhẹ, Tiny deployment profile (single binary, không RabbitMQ), migration
path generic-table-sang-bảng-riêng, computed/derived field, schema versioning cho entity, entity
variant kiểu polymorphic/discriminated-union, và metadata low-code theo từng Tenant (hướng dài
hạn cho Phase 11C's "quy tắc cô lập schema cấp Tenant", ghi lại 2026-08-22). Chi tiết và lý do
chưa lên phase ở `docs/team-charter.md`'s "Định hướng đang ghi nhận, chưa có trigger". Không bắt
đầu việc nào trong số này mà chưa có feature brief (`docs/features/`) nêu trigger cụ thể.
